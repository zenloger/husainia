services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: husaine-app
    restart: unless-stopped
    ports:
      - "127.0.0.1:5679:5679"
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
